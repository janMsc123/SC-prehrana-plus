fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
httpx==0.28.1
itsdangerous==2.2.0
cryptography==44.0.0
apscheduler==3.11.0
python-multipart==0.0.20
# Ships the IANA timezone database. python:*-slim images do not include the
# system tzdata package, so without this the cron schedule cannot resolve
# Europe/Ljubljana and the scheduler fails to start.
tzdata==2024.2
